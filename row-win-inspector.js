export default class RowWinInspector {
    constructor(columns){
        this.columns = columns;
        //[{column 1} , {column 2}, ...]
    }

    inspect(){
        for(let column = 0; column < this.columns.length; column++){
            let currentColumn = this.columns[column];
            currentColumn.tokens            
            for(){}
        }
    }
}